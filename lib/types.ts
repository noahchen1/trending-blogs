type SearchParams = {
    [index: string]: string;
    q: string;
    apiKey: string;
    sourtBy: string;
    pageSize: string;
}

type Content = {
    htmlContent: string;
    textContent: string;
}